services:
  postgres:
    image: postgres:16-alpine
    container_name: mini-saas-postgres
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mini_saas
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./migrations:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U admin -d mini_saas"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - mini-saas-network

  redis:
    image: redis:7-alpine
    container_name: mini-saas-redis
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - mini-saas-network

  api-gateway:
    build:
      context: .
      dockerfile: api-gateway/Dockerfile
    container_name: mini-saas-api-gateway
    environment:
      USER_SERVICE_URL: http://user-service:3001
      PROJECT_SERVICE_URL: http://project-service:3002
      BILLING_SERVICE_URL: http://billing-service:3003
      NOTIFICATION_SERVICE_URL: http://notification-service:3004
      JWT_SECRET: super-secret-key-must-be-32-chars-long-!!
      JWT_EXPIRATION: "3600"
    ports:
      - "3000:3000"
    depends_on:
      - user-service
      - project-service
      - billing-service
    networks:
      - mini-saas-network
    restart: on-failure

  user-service:
    build:
      context: .
      dockerfile: services/user-service/Dockerfile
    container_name: mini-saas-user-service
    environment:
      DATABASE_URL: postgresql://admin:password@postgres:5432/mini_saas
      JWT_SECRET: super-secret-key-must-be-32-chars-long-!!
      JWT_EXPIRATION: "3600"
    ports:
      - "3001:3001"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - mini-saas-network
    restart: on-failure

  project-service:
    build:
      context: .
      dockerfile: services/project-service/Dockerfile
    container_name: mini-saas-project-service
    environment:
      DATABASE_URL: postgresql://admin:password@postgres:5432/mini_saas
      JWT_SECRET: super-secret-key-must-be-32-chars-long-!!
    ports:
      - "3002:3002"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - mini-saas-network
    restart: on-failure

  billing-service:
    build:
      context: .
      dockerfile: services/billing-service/Dockerfile
    container_name: mini-saas-billing-service
    environment:
      DATABASE_URL: postgresql://admin:password@postgres:5432/mini_saas
      JWT_SECRET: super-secret-key-must-be-32-chars-long-!!
    ports:
      - "3003:3003"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - mini-saas-network
    restart: on-failure

  # notification-service:
  #   build:
  #     context: .
  #     dockerfile: services/notification-service/Dockerfile
  #   container_name: mini-saas-notification-service
  #   environment:
  #     DATABASE_URL: postgresql://admin:password@postgres:5432/mini_saas
  #     JWT_SECRET: super-secret-key-must-be-32-chars-long-!!
  #   ports:
  #     - "3004:3004"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks:
  #     - mini-saas-network

volumes:
  postgres_data:

networks:
  mini-saas-network:
    name: mini-saas-network
